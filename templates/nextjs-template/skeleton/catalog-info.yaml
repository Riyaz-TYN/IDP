apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  description: ${{ values.description }}
  annotations:
    backstage.io/techdocs-ref: dir:.
    github.com/project-slug: Riyaz-TYN/${{ values.name }}
spec:
  type: website
  lifecycle: experimental
  owner: ${{ values.owner }}
  system: labs

apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ parameters.name }}
  description: A frontend React application for ${{ parameters.name }}
  tags:
    - react
    - frontend
spec:
  type: website
  lifecycle: experimental
  owner: user:guest

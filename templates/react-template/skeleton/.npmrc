# @riyaz-tyn scoped packages are hosted on GitHub Packages.
# This read-only token lets you install packages without any login.
# It has ONLY read:packages permission — it cannot access code or secrets.
@riyaz-tyn:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_PACKAGES_TOKEN}
